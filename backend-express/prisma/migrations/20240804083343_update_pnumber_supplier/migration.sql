BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[supplier] ALTER COLUMN [phone_number] VARCHAR(12) NOT NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
